module(load="imtcp")
input(type="imtcp" port="514")

module(load="imudp")
input(type="imudp" port="514")

template(
    name="RemoteLogs"
    type="string"
    string="/logs/%$YEAR%-%$MONTH%-%$DAY%/%programname%-%hostname%.txt"
)

*.* ?RemoteLogs

& stop